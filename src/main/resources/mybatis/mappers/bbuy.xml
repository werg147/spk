<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="bbuy">
	
	<insert id="insertBBuy">
	 <![CDATA[
			insert into bbuy
			values( seq_bbuy_no.nextval,
			        #{user_no},
			        #{booking_no},
			        #{profile_no},
			        #{b_buy_paytype},
			        #{price},
			        #{b_buy_state},
			        #{b_buy_player_state},
			        sysdate)	 	
	 
	 ]]>
	</insert>
	
</mapper>